{
  "name": "recall-engine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "ingest": "npx tsx src/scripts/ingest.ts",
    "ingest:makes": "npx tsx src/scripts/ingest.ts --makes-only",
    "ingest:recalls": "npx tsx src/scripts/ingest.ts --recalls"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "prisma": "^6.1.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.5"
  }
}
